[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: requires live PostgreSQL and API keys (skip with -m "not integration")
    synera: requires Synera headless executable to be installed
