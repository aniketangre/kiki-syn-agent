# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# LangGraph / SQLite database
conversations.db
conversations.db-shm
conversations.db-wal

# Generated images from Synera tool runs
images/
output/

# Chroma vector store — built locally by tools/rag_search/ingest.py
chroma_db/

# Docker overrides with local settings (e.g. different ports, bind mounts)
docker-compose.override.yml

# Generated diagrams
diagrams/

# Synera tool output files (generated at runtime)
tools/*/Results/
tools/*/*_output.json
synera_run/output.json
synera_run/test_output.json

# Claude Code local settings (machine-specific permissions, not project config)
.claude/

# OS
.DS_Store
Thumbs.db
